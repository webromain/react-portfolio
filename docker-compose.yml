version: '3'

services:
  portfolio:
    build: /home/romain-poisson/docker/react-portfolio
    container_name: portfolio-app
    restart: always
    environment:
      - NODE_ENV=production
      - VIRTUAL_HOST=portfolio.romainpoisson.com
      - LETSENCRYPT_HOST=portfolio.romainpoisson.com
      - LETSENCRYPT_EMAIL=admin@romainpoisson.com
    networks:
      - nginx-proxy
      - default

networks:
  nginx-proxy:
    external: true